apiVersion: v1
kind: Service
metadata:
  name: ghslacker
  labels:
    app: ghslacker
spec:
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 5000
  selector:
    app: ghslacker
